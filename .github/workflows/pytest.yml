name: Run tests

on: [push, pull_request]


jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout sources
        uses: actions/checkout@v2

      - name: pytest
        uses: docker://ghcr.io/intyamo/pytester
